apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: accounts
spec:
  # Headless: da identidad de red estable al Pod del StatefulSet (postgres-0.postgres.accounts.svc).
  clusterIP: None
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
